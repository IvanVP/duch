<section class="sliderwrapper clearfix">
  <div class="tp-banner-container">
    <div class="tp-banner" >
      <% if home_page? %>
        <img width="100%" src= <%= image_path "main1.jpg" %> alt="Движение к истоку"></a>
      <% else %>
        <img width="100%" src=<%= image_path "maincut.jpg" %> alt="Врезка - Движение к истоку">
      <% end %>
    </div>
  </div>       
</section>
