 <%= render :partial => '/refinery/menu_side',
      :locals => {
      :dom_id => 'submenu',
      :css => 'menu',
      :roots => refinery_menu_pages.reject{|p| p.parent_id != ((@page.root.id if @page) || (Refinery::Portfolio::Gallery.root.id))}
    } %>



