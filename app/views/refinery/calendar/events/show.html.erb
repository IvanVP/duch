<%= render @event, state: :full %>

<% render 'refinery/calendar/categories/categories_list' %>

<% content_for :stylesheets, stylesheet_link_tag('events') %>

<%#= render '/refinery/content_page' %>
